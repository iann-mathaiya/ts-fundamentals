"use strict";
// Tuples help having an array that contains values iwth mixed types
let person1 = ['Marcia', 35];
// Elements in a tuple array are fixed hence the boolean causes an error
// let person2: [string, number] = ['Marcia', 35, true]; 
// The error that indicates that the order of the values must match the order of the types
// let person3: [string, number] = [35, 'Marcia']; 
